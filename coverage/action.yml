name: 'actions/coverage'
description: 'Coverage project'
inputs:
  gist-id:
    description: gist id
    required: true

  gist-token:
    description: gist token
    required: true

branding:
  icon: 'box'
  color: 'green'

runs:
  using: "composite"
  steps:

    - uses: actions/checkout@v5

    - id: project_type
      uses: yadickson/actions-steps/project-type@v1

    - uses: yadickson/actions-steps/cache-dependencies@v1

    - if: steps.project_type.outputs.type == 'node'
      id: node-coverage
      uses: yadickson/actions-steps/project-coverage/node@v1
      continue-on-error: true

    - if: steps.project_type.outputs.type == 'python'
      id: python-coverage
      uses: yadickson/actions-steps/project-coverage/python@v1
      continue-on-error: true

    - if: github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
      id: badge-coverage
      uses: yadickson/actions-steps/project-badge/coverage@v1
      with:
        gist-id: ${{ inputs.gist-id }}
        gist-token: ${{ inputs.gist-token }}

    - name: coverage project fail
      if: steps.node-coverage.outcome == 'failure' || steps.python-coverage.outcome == 'failure'
      run: exit 1
      shell: bash

